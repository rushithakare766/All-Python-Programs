# gcd of two numbers 
import math
a=int(input("Enter the number:")) # taking value from user
b=int(input("Enter the number:")) # taking value from user
print(math.gcd(a,b)) # print the value of  gcd of two number
